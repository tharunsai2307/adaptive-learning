"""Database Management Systems curriculum (B.Tech IT 2nd Year)."""

TOPICS = [
    {
        "name": "Introduction to DBMS",
        "difficulty": "easy",
        "order_number": 1,
        "description": "Why databases exist, the three-schema architecture, and DBMS responsibilities.",
        "introduction": "A DBMS is software that stores, retrieves, and manages data while guaranteeing correctness under concurrent access and failures. It replaces ad-hoc file storage with a system that enforces structure, integrity, and shared access.",
        "explanation": "**Problems with file-based storage**\nData redundancy and inconsistency, difficult concurrent access, poor security, no crash recovery, and data isolation between programs.\n\n**What a DBMS provides**\n- Data abstraction — hide physical storage details\n- Concurrency control — many users, consistent results\n- Recovery — undo/redo after a crash\n- Integrity constraints — the data obeys the rules\n- Security and access control\n\n**Three-schema architecture**\n1. Physical/internal — how bytes are stored on disk (indexes, pages)\n2. Logical/conceptual — tables, columns, relationships\n3. External/view — what each user or application sees\n\n**Data independence**\n- Logical — change the schema without breaking applications\n- Physical — change storage or indexes without changing the schema\n\n**Data models**: relational (tables), hierarchical, network, object-oriented, and NoSQL (document, key-value, column-family, graph).\n\n**DBMS vs RDBMS**: an RDBMS stores data in relations (tables) and is accessed with SQL.",
        "basic_example": "A library file 'books.txt' stores each book's title, author, and borrower. When a book is loaned you edit one line. If the same title appears twice, updating one copy leaves the other stale — that is an update anomaly. A DBMS stores the title once and references it.",
        "advanced_example": "Data independence in practice: adding an index on customers(email) can make a query 1000x faster, yet no application code changes — that is physical data independence. Likewise, splitting an `orders` table into `orders` and `order_items` can be hidden behind a view named `orders`, so existing queries keep working — logical data independence.",
        "key_points": "• A DBMS removes redundancy, and adds concurrency, recovery, integrity, and security\n• Three schemas: physical, logical, external\n• Physical independence = change storage freely; logical = change schema freely\n• The relational model organises data into tables (relations)\n• DBMS roles: storage manager and query processor",
        "resources": "• 'Database System Concepts' — Silberschatz, chapters 1-2\n• 'Fundamentals of Database Systems' — Elmasri & Navathe\n• Gate Smashers DBMS playlist\n• PostgreSQL or SQLite documentation",
        "study_time_minutes": 30,
    },
    {
        "name": "The Relational Model and ER Diagrams",
        "difficulty": "medium",
        "order_number": 2,
        "description": "Relations, keys, integrity rules, and modelling with entity-relationship diagrams.",
        "introduction": "The relational model represents data as relations — tables of rows and columns with no duplicate rows and no inherent ordering. ER diagrams are how we design those tables before writing any SQL.",
        "explanation": "**Relation terminology**\nrelation = table, tuple = row, attribute = column, domain = allowed values, degree = number of columns, cardinality = number of rows.\n\n**Keys**\n- Superkey — a set of attributes that uniquely identifies a row\n- Candidate key — a minimal superkey\n- Primary key — the chosen candidate key (never NULL)\n- Foreign key — references the primary key of another table\n- Composite key — a primary key made of multiple columns\n\n**Integrity constraints**\n- Entity integrity: no primary key attribute may be NULL\n- Referential integrity: a foreign key must match an existing row or be NULL\n- Domain, key, and user-defined constraints\n\n**ER modelling**\nEntities (rectangles), attributes (ovals), relationships (diamonds). Relationship cardinality is one-to-one, one-to-many, or many-to-many. A many-to-many relationship must become a junction table when implemented.\n\n**Weak entities** depend on an owner entity and use a partial key plus the owner's key.\n\n**Mapping rules**: each entity becomes a table; a 1:N relationship adds a foreign key on the 'many' side; an M:N relationship becomes its own table with two foreign keys.",
        "basic_example": "ER: STUDENT enrolls in COURSE (many-to-many).\n\nRelational mapping:\n  STUDENT(student_id PK, name, email)\n  COURSE(course_id PK, title, credits)\n  ENROLLMENT(student_id FK, course_id FK, semester,\n             PRIMARY KEY(student_id, course_id))\n\nThe junction table ENROLLMENT resolves the M:N relationship and can carry its own attributes, like `grade`.",
        "advanced_example": "Consider ORDERS and ORDER_ITEMS. Putting the item list inside the order row forces a fixed maximum and repeats the order's customer and date for every item — a classic update anomaly. Normalising to two tables means the customer name is stored once, an item can be corrected in one place, and the order total is derived rather than duplicated:\n\n  ORDERS(order_id PK, customer_id FK, order_date, total)\n  ORDER_ITEMS(item_id PK, order_id FK, product_id FK, qty, unit_price)",
        "key_points": "• A relation has no duplicate tuples and no ordering of rows\n• A primary key is a minimal unique identifier and is never NULL\n• A foreign key enforces referential integrity between tables\n• M:N relationships become junction tables\n• Weak entities need the owner's key to be identified",
        "resources": "• Elmasri & Navathe — ER modelling chapters\n• Lucidchart ER diagram guide\n• 'Database Design for Mere Mortals'\n• draw.io for free ER diagrams",
        "study_time_minutes": 45,
    },
    {
        "name": "SQL: Queries and Joins",
        "difficulty": "medium",
        "order_number": 3,
        "description": "SELECT, filtering, aggregation, joins, subqueries, and the order of evaluation.",
        "introduction": "SQL is the declarative language for relational databases. You describe the result you want, and the query optimiser decides how to produce it — which is why understanding evaluation order matters more than syntax.",
        "explanation": "**Logical evaluation order** (not the order you write it)\n    FROM -> JOIN/ON -> WHERE -> GROUP BY -> HAVING -> SELECT -> DISTINCT -> ORDER BY -> LIMIT\n\nThis explains two common errors: you cannot use a column alias in WHERE (SELECT has not run yet), and you cannot filter on an aggregate in WHERE — use HAVING.\n\n**Joins**\n- INNER JOIN — only matching rows\n- LEFT JOIN — all left rows, NULLs where no match\n- RIGHT JOIN — mirror of LEFT\n- FULL OUTER JOIN — both sides preserved\n- CROSS JOIN — cartesian product\n- SELF JOIN — a table joined to itself (e.g. employee to manager)\n\n**Aggregation**\n    SELECT dept, COUNT(*) n, AVG(salary) avg_sal\n    FROM employees\n    GROUP BY dept\n    HAVING COUNT(*) > 5;\n\n**NULL semantics**: NULL is unknown, so `salary != 100` excludes NULL rows. Use `IS NULL` / `IS NOT NULL`, and remember `COUNT(*)` counts rows while `COUNT(col)` skips NULLs.\n\n**Set operations** UNION (distinct), UNION ALL, INTERSECT, EXCEPT.",
        "basic_example": "SELECT s.name, c.title\nFROM students s\nJOIN enrollment e ON e.student_id = s.id\nJOIN courses   c ON c.id = e.course_id\nWHERE s.year = 2\nORDER BY s.name;",
        "advanced_example": "To find students enrolled in NO course, a LEFT JOIN with a NULL check is usually faster and clearer than NOT IN — and NOT IN silently returns zero rows if any subquery value is NULL:\n\n    -- Safe\n    SELECT s.name\n    FROM students s\n    LEFT JOIN enrollment e ON e.student_id = s.id\n    WHERE e.student_id IS NULL;\n\n    -- Dangerous: returns nothing if any e.student_id is NULL\n    SELECT name FROM students\n    WHERE id NOT IN (SELECT student_id FROM enrollment);\n\nUse NOT EXISTS if you prefer a subquery — it handles NULL correctly.",
        "key_points": "• Logical order is FROM, WHERE, GROUP BY, HAVING, SELECT, ORDER BY\n• HAVING filters groups; WHERE filters rows\n• COUNT(*) counts rows, COUNT(col) ignores NULLs\n• NOT IN breaks on NULLs — prefer NOT EXISTS or a LEFT JOIN check\n• Indexes speed up WHERE and JOIN columns",
        "resources": "• SQLBolt interactive tutorial\n• 'SQL in 10 Minutes a Day' — Ben Forta\n• PostgreSQL tutorial\n• LeetCode SQL problem set",
        "study_time_minutes": 45,
    },
    {
        "name": "Normalization",
        "difficulty": "hard",
        "order_number": 4,
        "description": "Functional dependencies and 1NF through BCNF to eliminate redundancy.",
        "introduction": "Normalization is the process of structuring tables to remove redundancy and the update, insertion, and deletion anomalies it causes. Each normal form adds one constraint based on functional dependencies.",
        "explanation": "**Anomalies**\n- Update: the same fact stored twice gets changed in one place only\n- Insertion: you cannot record a fact without unrelated data\n- Deletion: removing a row loses other information\n\n**Functional dependency** X -> Y means the value of X determines the value of Y.\n\n**1NF** — every column holds atomic (indivisible) values; no repeating groups.\n\n**2NF** — 1NF plus no *partial* dependency: no non-key attribute depends on only part of a composite primary key.\n\n**3NF** — 2NF plus no *transitive* dependency: non-key attributes must not depend on other non-key attributes.\n\n**BCNF** — for every dependency X -> Y, X must be a superkey. Stricter than 3NF.\n\n**Higher forms** 4NF removes multi-valued dependencies; 5NF removes join dependencies.\n\n**Denormalisation** deliberately reintroduces redundancy for read performance — acceptable once measurements show it is needed, and it must be documented.",
        "basic_example": "Unnormalised:\n  STUDENT_COURSE(student_id, name, course_id, course_title, instructor)\n\n'name' repeats per course (update anomaly); a course with no students cannot be recorded (insertion anomaly).\n\nIn 3NF:\n  STUDENT(student_id, name)\n  COURSE(course_id, course_title, instructor)\n  ENROLLMENT(student_id, course_id)",
        "advanced_example": "A table that already satisfies 3NF can still violate BCNF. Suppose COURSE_ALLOCATION(course, instructor, room) with:\n  (course, instructor) -> room\n  instructor -> room          (each instructor has one fixed room)\n  (course, room) -> instructor\n\nCandidate keys are (course, instructor) and (course, room). The dependency instructor -> room has a non-superkey determinant, so this is 3NF but not BCNF. Decomposing into (course, instructor) and (instructor, room) resolves it without losing information.",
        "key_points": "• Normalization removes update, insertion, and deletion anomalies\n• 1NF: atomic values; 2NF: no partial dependency; 3NF: no transitive dependency\n• BCNF: every determinant must be a superkey\n• A relation in BCNF is also in 3NF, but not the reverse\n• Decomposition must be lossless and preserve dependencies\n• Denormalise only when measurements justify it",
        "resources": "• Elmasri & Navathe — Relational Design and Normalization\n• Gate Smashers normalization playlist\n• 'SQL Antipatterns' — Bill Karwin\n• Practice converting unnormalised forms to 3NF by hand",
        "study_time_minutes": 50,
    },
    {
        "name": "Transactions, Indexes, and Concurrency",
        "difficulty": "hard",
        "order_number": 5,
        "description": "ACID properties, isolation levels, locking, and index structures.",
        "introduction": "A transaction is a unit of work that either fully succeeds or has no effect. ACID is what lets concurrent users share a database safely, and indexes are what make its queries fast.",
        "explanation": "**ACID**\n- Atomicity — all of it or none of it\n- Consistency — the database moves between valid states\n- Isolation — concurrent transactions do not interfere\n- Durability — committed changes survive crashes\n\n**Concurrency problems**\n- Dirty read — reading another transaction's uncommitted change\n- Non-repeatable read — a row changes between two reads\n- Phantom read — new rows appear between two reads\n- Lost update — two writes overwrite each other\n\n**Isolation levels** (weakest to strongest): READ UNCOMMITTED, READ COMMITTED, REPEATABLE READ, SERIALIZABLE. Higher isolation costs throughput.\n\n**Locks**: shared (read) and exclusive (write); two-phase locking acquires all locks before releasing any. Deadlock happens when two transactions wait on each other — resolved by detection and aborting one.\n\n**Indexes**\nA B+ tree index keeps keys sorted with all data in the leaves, giving O(log n) lookup and efficient range scans. Indexes speed reads but slow writes, since every insert/update must maintain them.\n\n**Transaction control**: BEGIN, COMMIT, ROLLBACK, SAVEPOINT.",
        "basic_example": "BEGIN;\nUPDATE accounts SET balance = balance - 500 WHERE id = 1;\nUPDATE accounts SET balance = balance + 500 WHERE id = 2;\nCOMMIT;\n\n-- If anything fails:\nROLLBACK;   -- neither update is visible",
        "advanced_example": "The classic lost update: two sessions read the same balance of 100, each adds 50, and both write 150 — one update vanishes. Under READ COMMITTED this is possible. Fixes:\n\n  -- 1. Atomic in-place update (preferred)\n  UPDATE accounts SET balance = balance + 50 WHERE id = 1;\n\n  -- 2. Pessimistic lock\n  SELECT balance FROM accounts WHERE id = 1 FOR UPDATE;\n\n  -- 3. Optimistic concurrency control\n  UPDATE accounts SET balance = 150, version = version + 1\n  WHERE id = 1 AND version = 7;   -- 0 rows affected => retry\n\nOption 3 avoids holding locks and suits high-contention web workloads.",
        "key_points": "• ACID: Atomicity, Consistency, Isolation, Durability\n• Dirty, non-repeatable, and phantom reads map to isolation levels\n• Two-phase locking prevents lost updates but can deadlock\n• B+ tree indexes give O(log n) lookups and fast range scans\n• Indexes accelerate reads and slow down writes\n• COMMIT makes changes durable; ROLLBACK discards them",
        "resources": "• Silberschatz — Transactions and Concurrency Control\n• 'Designing Data-Intensive Applications' — Kleppmann, ch. 7\n• PostgreSQL docs — MVCC and isolation levels\n• Use EXPLAIN ANALYZE to see whether an index is used",
        "study_time_minutes": 50,
    },
]

QUESTIONS = {
    "Introduction to DBMS": [
        {"question": "Which is NOT a responsibility of a DBMS?", "option_a": "Concurrency control", "option_b": "Crash recovery", "option_c": "Compiling application source code", "option_d": "Enforcing integrity constraints", "correct_answer": "C", "explanation": "Compiling is done by a language toolchain, not the database."},
        {"question": "In the three-schema architecture, which schema describes tables and columns?", "option_a": "Physical", "option_b": "Logical/conceptual", "option_c": "External/view", "option_d": "Network", "correct_answer": "B", "explanation": "The logical schema defines the structure users reason about; the physical schema is about storage."},
        {"question": "Changing indexes without touching application code demonstrates:", "option_a": "Logical data independence", "option_b": "Physical data independence", "option_c": "Concurrency control", "option_d": "Normalization", "correct_answer": "B", "explanation": "Physical independence means storage-level changes do not affect higher schemas."},
        {"question": "Storing the same fact in two places that drift apart is called:", "option_a": "Data independence", "option_b": "Data inconsistency from redundancy", "option_c": "Referential integrity", "option_d": "Atomicity", "correct_answer": "B", "explanation": "Redundancy without coordination leads to inconsistent copies of the same fact."},
        {"question": "The relational model organises data into:", "option_a": "Trees", "option_b": "Graphs only", "option_c": "Relations (tables)", "option_d": "Hierarchies", "correct_answer": "C", "explanation": "Relations are two-dimensional tables of rows and columns."},
        {"question": "Which is a NoSQL data model?", "option_a": "Relational", "option_b": "Document", "option_c": "Hierarchical", "option_d": "Network", "correct_answer": "B", "explanation": "Document, key-value, column-family, and graph stores are NoSQL models."},
        {"question": "An external schema describes:", "option_a": "Disk layout", "option_b": "The view seen by a particular user or application", "option_c": "The full logical design", "option_d": "Backup files", "correct_answer": "B", "explanation": "Each external schema is a tailored view over the logical schema."},
        {"question": "Which DBMS feature undoes partial work after a crash?", "option_a": "Query optimisation", "option_b": "Recovery with undo/redo logging", "option_c": "Indexing", "option_d": "Views", "correct_answer": "B", "explanation": "Write-ahead logging allows rolling back uncommitted and replaying committed transactions."},
        {"question": "RDBMS differs from a generic DBMS because it:", "option_a": "Cannot handle SQL", "option_b": "Stores data in relations accessed via SQL", "option_c": "Has no integrity rules", "option_d": "Runs only on mainframes", "correct_answer": "B", "explanation": "The 'R' means the data is modelled as relations and manipulated with SQL."},
        {"question": "Data abstraction in a DBMS means:", "option_a": "Deleting unused data", "option_b": "Hiding physical storage details from users", "option_c": "Compressing data", "option_d": "Encrypting data", "correct_answer": "B", "explanation": "Users work with logical concepts, not pages, blocks, or file offsets."},
    ],
    "The Relational Model and ER Diagrams": [
        {"question": "A minimal superkey is called a:", "option_a": "Foreign key", "option_b": "Candidate key", "option_c": "Composite index", "option_d": "Domain", "correct_answer": "B", "explanation": "A candidate key uniquely identifies a row with no redundant attributes."},
        {"question": "Entity integrity states that:", "option_a": "Foreign keys must be unique", "option_b": "No primary key attribute may be NULL", "option_c": "All columns must be indexed", "option_d": "Tables must have exactly one key", "correct_answer": "B", "explanation": "A NULL primary key would make the row unidentifiable."},
        {"question": "Referential integrity requires a foreign key value to:", "option_a": "Be unique", "option_b": "Match an existing referenced row or be NULL", "option_c": "Never be NULL", "option_d": "Be numeric", "correct_answer": "B", "explanation": "It prevents references to rows that do not exist."},
        {"question": "A many-to-many relationship is implemented as:", "option_a": "A foreign key on either side", "option_b": "A junction table with two foreign keys", "option_c": "An index", "option_d": "A view only", "correct_answer": "B", "explanation": "The junction (associative) table holds one row per relationship instance."},
        {"question": "The number of rows in a relation is its:", "option_a": "Degree", "option_b": "Cardinality", "option_c": "Domain", "option_d": "Schema", "correct_answer": "B", "explanation": "Cardinality counts tuples; degree counts attributes."},
        {"question": "In an ER diagram, a diamond represents:", "option_a": "An entity", "option_b": "An attribute", "option_c": "A relationship", "option_d": "A primary key", "correct_answer": "C", "explanation": "Rectangles are entities, ovals are attributes, diamonds are relationships."},
        {"question": "A weak entity is identified by:", "option_a": "Its own unique key alone", "option_b": "A partial key together with the owner entity's key", "option_c": "An index", "option_d": "A foreign key only", "correct_answer": "B", "explanation": "It cannot be identified without the entity it depends on."},
        {"question": "Which is true of a relation?", "option_a": "Duplicate tuples are allowed", "option_b": "Row order is significant", "option_c": "Tuples are unique and unordered", "option_d": "Attributes may repeat", "correct_answer": "C", "explanation": "Relations are sets of tuples, so there are no duplicates and no inherent order."},
        {"question": "In a 1:N relationship, the foreign key is placed on:", "option_a": "The 'one' side", "option_b": "The 'many' side", "option_c": "Both sides", "option_d": "A separate table always", "correct_answer": "B", "explanation": "Each row on the many side points to its single related row."},
        {"question": "A composite primary key is one that:", "option_a": "Contains a foreign key", "option_b": "Consists of more than one attribute", "option_c": "Is auto-generated", "option_d": "Is always numeric", "correct_answer": "B", "explanation": "Multiple columns combine to form the unique identifier."},
    ],
    "SQL: Queries and Joins": [
        {"question": "In SQL's logical evaluation order, which clause runs first?", "option_a": "SELECT", "option_b": "FROM", "option_c": "WHERE", "option_d": "ORDER BY", "correct_answer": "B", "explanation": "FROM establishes the rows; everything else filters or shapes them."},
        {"question": "Filtering on an aggregate such as COUNT(*) requires:", "option_a": "WHERE", "option_b": "HAVING", "option_c": "ORDER BY", "option_d": "LIMIT", "correct_answer": "B", "explanation": "WHERE runs before grouping, so aggregate conditions belong in HAVING."},
        {"question": "Which join keeps all rows from the left table even with no match?", "option_a": "INNER JOIN", "option_b": "LEFT JOIN", "option_c": "CROSS JOIN", "option_d": "SELF JOIN", "correct_answer": "B", "explanation": "A LEFT (OUTER) JOIN preserves unmatched left rows with NULLs for the right side."},
        {"question": "What does COUNT(column) do differently from COUNT(*)?", "option_a": "Nothing", "option_b": "COUNT(column) ignores NULLs in that column", "option_c": "COUNT(*) ignores duplicates", "option_d": "COUNT(column) is faster always", "correct_answer": "B", "explanation": "COUNT(*) counts rows; COUNT(col) counts non-NULL values of col."},
        {"question": "Why is `NOT IN (subquery)` risky?", "option_a": "It is slow only", "option_b": "It returns no rows if the subquery yields any NULL", "option_c": "It requires an index", "option_d": "It cannot be nested", "correct_answer": "B", "explanation": "Comparison with NULL is unknown, so the whole predicate fails; use NOT EXISTS."},
        {"question": "Which clause removes duplicate rows from the result?", "option_a": "UNIQUE", "option_b": "DISTINCT", "option_c": "GROUP", "option_d": "FILTER", "correct_answer": "B", "explanation": "DISTINCT collapses duplicate result rows."},
        {"question": "A cartesian product results from:", "option_a": "INNER JOIN with ON", "option_b": "CROSS JOIN", "option_c": "LEFT JOIN", "option_d": "GROUP BY", "correct_answer": "B", "explanation": "CROSS JOIN pairs every row of one table with every row of the other."},
        {"question": "Which set operator keeps duplicates?", "option_a": "UNION", "option_b": "UNION ALL", "option_c": "INTERSECT", "option_d": "EXCEPT", "correct_answer": "B", "explanation": "UNION removes duplicates; UNION ALL preserves them and is cheaper."},
        {"question": "You cannot reference a SELECT alias in WHERE because:", "option_a": "Aliases are not allowed", "option_b": "WHERE is evaluated before SELECT", "option_c": "Aliases only work in joins", "option_d": "It is a syntax restriction only", "correct_answer": "B", "explanation": "Logical order puts WHERE before SELECT, so the alias does not exist yet."},
        {"question": "A self join is used to:", "option_a": "Duplicate a table", "option_b": "Relate rows of a table to other rows of the same table", "option_c": "Create an index", "option_d": "Delete duplicates", "correct_answer": "B", "explanation": "Common for hierarchies, such as matching an employee to their manager."},
    ],
    "Normalization": [
        {"question": "First Normal Form (1NF) requires:", "option_a": "No transitive dependencies", "option_b": "Atomic values and no repeating groups", "option_c": "A composite key", "option_d": "All keys to be foreign keys", "correct_answer": "B", "explanation": "Every attribute must hold a single indivisible value."},
        {"question": "2NF eliminates:", "option_a": "Transitive dependencies", "option_b": "Partial dependencies on a composite key", "option_c": "Multi-valued dependencies", "option_d": "All redundancy", "correct_answer": "B", "explanation": "No non-key attribute may depend on only part of a composite primary key."},
        {"question": "3NF eliminates:", "option_a": "Partial dependencies", "option_b": "Transitive dependencies between non-key attributes", "option_c": "Join dependencies", "option_d": "Foreign keys", "correct_answer": "B", "explanation": "Non-key attributes must depend only on the key, not on other non-key attributes."},
        {"question": "BCNF requires that for every dependency X -> Y:", "option_a": "Y is a key", "option_b": "X is a superkey", "option_c": "X is a single attribute", "option_d": "Y is atomic", "correct_answer": "B", "explanation": "Every determinant must be a superkey, which is stricter than 3NF."},
        {"question": "Which is true?", "option_a": "Every 3NF relation is in BCNF", "option_b": "Every BCNF relation is in 3NF", "option_c": "They are unrelated", "option_d": "BCNF allows partial dependencies", "correct_answer": "B", "explanation": "BCNF is a stronger condition, so it implies 3NF but not the reverse."},
        {"question": "An update anomaly occurs when:", "option_a": "A table has no primary key", "option_b": "The same fact is stored in several places and only some are updated", "option_c": "A query is slow", "option_d": "A transaction fails", "correct_answer": "B", "explanation": "Redundant copies drift apart, producing inconsistent data."},
        {"question": "A functional dependency X -> Y means:", "option_a": "Y determines X", "option_b": "The value of X uniquely determines the value of Y", "option_c": "X and Y are equal", "option_d": "Y is a key", "correct_answer": "B", "explanation": "Knowing X tells you exactly one value of Y."},
        {"question": "A decomposition must be:", "option_a": "Lossy but fast", "option_b": "Lossless and dependency preserving", "option_c": "Always to BCNF", "option_d": "Irreversible", "correct_answer": "B", "explanation": "The original relation must be reconstructible, and the constraints must still be enforceable."},
        {"question": "Denormalisation is:", "option_a": "Always a design error", "option_b": "Deliberately adding redundancy to improve read performance", "option_c": "Removing all keys", "option_d": "The same as 1NF", "correct_answer": "B", "explanation": "It is a measured trade-off that must be justified and documented."},
        {"question": "4NF addresses:", "option_a": "Transitive dependencies", "option_b": "Multi-valued dependencies", "option_c": "Partial dependencies", "option_d": "Atomicity", "correct_answer": "B", "explanation": "4NF removes independent multi-valued dependencies from a single relation."},
    ],
    "Transactions, Indexes, and Concurrency": [
        {"question": "ACID's 'A' (Atomicity) means:", "option_a": "Changes are permanent", "option_b": "A transaction is all-or-nothing", "option_c": "Transactions run alone", "option_d": "Data is always valid", "correct_answer": "B", "explanation": "Either every operation commits or none of them take effect."},
        {"question": "Reading another transaction's uncommitted change is a:", "option_a": "Phantom read", "option_b": "Dirty read", "option_c": "Lost update", "option_d": "Deadlock", "correct_answer": "B", "explanation": "Dirty reads come from uncommitted data that may later be rolled back."},
        {"question": "The strongest isolation level is:", "option_a": "READ UNCOMMITTED", "option_b": "READ COMMITTED", "option_c": "REPEATABLE READ", "option_d": "SERIALIZABLE", "correct_answer": "D", "explanation": "SERIALIZABLE behaves as if transactions ran one at a time, at the highest cost."},
        {"question": "A deadlock occurs when:", "option_a": "A query is slow", "option_b": "Two transactions each wait for a lock held by the other", "option_c": "An index is missing", "option_d": "A transaction commits twice", "correct_answer": "B", "explanation": "The wait cycle cannot resolve itself, so the DBMS aborts one transaction."},
        {"question": "Two-phase locking means:", "option_a": "Two locks per row", "option_b": "All locks are acquired in a growing phase before any are released", "option_c": "Locking twice for safety", "option_d": "Read and write locks only", "correct_answer": "B", "explanation": "The growing then shrinking phase discipline guarantees serializability."},
        {"question": "A B+ tree index stores actual data:", "option_a": "In every node", "option_b": "Only in the leaf nodes", "option_c": "In the root only", "option_d": "Nowhere", "correct_answer": "B", "explanation": "Internal nodes hold only keys for navigation, so leaves link together for range scans."},
        {"question": "Adding an index on a heavily written column will:", "option_a": "Speed up inserts", "option_b": "Slow down inserts and updates", "option_c": "Have no effect", "option_d": "Reduce disk usage", "correct_answer": "B", "explanation": "Every modification must also maintain the index structure."},
        {"question": "Durability is usually implemented with:", "option_a": "Caching", "option_b": "Write-ahead logging", "option_c": "Indexing", "option_d": "Views", "correct_answer": "B", "explanation": "The log is flushed before commit, so committed work survives a crash."},
        {"question": "ROLLBACK does what?", "option_a": "Commits pending changes", "option_b": "Discards the current transaction's changes", "option_c": "Restarts the server", "option_d": "Creates a savepoint", "correct_answer": "B", "explanation": "It undoes everything done since BEGIN or the last SAVEPOINT."},
        {"question": "Which technique avoids holding locks during a read-modify-write?", "option_a": "SELECT ... FOR UPDATE", "option_b": "Optimistic concurrency control with a version column", "option_c": "SERIALIZABLE isolation", "option_d": "Two-phase locking", "correct_answer": "B", "explanation": "The update is conditional on an unchanged version; a conflict is detected at write time and retried."},
    ],
}
