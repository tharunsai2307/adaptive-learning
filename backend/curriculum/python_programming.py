"""Python Programming curriculum (B.Tech / BCA 1st Year)."""

TOPICS = [
    {
        "name": "Introduction to Python",
        "difficulty": "easy",
        "order_number": 1,
        "description": "Python syntax basics: variables, data types, operators, input and output.",
        "introduction": "Python is a high-level, interpreted, general-purpose programming language designed around readability. Its syntax lets you express ideas in fewer lines than most languages, which is why it dominates teaching, data science, and scripting.",
        "explanation": "Python code is executed line by line by an interpreter, so there is no separate compile step.\n\n**Variables and types**\nPython is dynamically typed — you do not declare a type, and a name can be rebound to a value of another type.\n\n    name = 'Ada'        # str\n    age = 36            # int\n    height = 1.68       # float\n    is_active = True    # bool\n\n**Core built-in types**\n- `int`, `float` — numbers\n- `str` — immutable text\n- `bool` — True / False\n- `list`, `tuple`, `dict`, `set` — collections\n\n**Operators**\nArithmetic (`+ - * / // % **`), comparison (`== != < > <= >=`), and logical (`and or not`). Note that `/` always returns a float, while `//` is floor division.\n\n**Input / output**\n    name = input('Your name: ')   # always returns str\n    print(f'Hello, {name}')       # f-string interpolation",
        "basic_example": "age = input('Enter your age: ')\nage = int(age)\nif age >= 18:\n    print('You can vote.')\nelse:\n    print('You cannot vote yet.')",
        "advanced_example": "Type hints do not change runtime behaviour but are checked by tools like mypy and improve editor support:\n\ndef bmi(weight: float, height: float) -> float:\n    return weight / height ** 2\n\nprint(f'{bmi(70, 1.75):.1f}')   # 22.9\n\nCombined with dataclasses this gives Python much of the safety of a statically typed language while staying readable.",
        "key_points": "• Interpreted, dynamically typed, indentation defines blocks\n• `/` returns float, `//` is floor division, `**` is power\n• f-strings (`f'{x}'`) are the modern way to format output\n• `input()` always returns a string — convert it explicitly\n• `type(x)` and `isinstance(x, T)` inspect types at runtime",
        "resources": "• Official Python Tutorial (docs.python.org)\n• 'Automate the Boring Stuff with Python' — free online\n• Python for Everybody (Coursera)\n• Real Python — Basics track",
        "study_time_minutes": 30,
    },
    {
        "name": "Control Flow",
        "difficulty": "easy",
        "order_number": 2,
        "description": "Conditionals, loops, break/continue, and comprehensions.",
        "introduction": "Control flow decides which statements run and how often. Python keeps this simple with `if/elif/else`, two loop constructs (`for` and `while`), and a concise comprehension syntax for building collections.",
        "explanation": "**Conditionals**\n    if score >= 80:\n        grade = 'A'\n    elif score >= 60:\n        grade = 'B'\n    else:\n        grade = 'F'\n\n**for loops** iterate over any iterable — lists, strings, ranges, files:\n    for i in range(5):      # 0, 1, 2, 3, 4\n        print(i)\n\n**while loops** run until a condition fails:\n    n = 10\n    while n > 0:\n        n -= 1\n\n**break / continue / else**\n- `break` exits the loop immediately\n- `continue` skips to the next iteration\n- a `for ... else:` block runs only if the loop was NOT broken\n\n**Comprehensions** replace many loops with one readable expression:\n    squares = [x ** 2 for x in range(10) if x % 2 == 0]\n\nAvoid deeply nested loops; extract a function instead.",
        "basic_example": "for n in range(1, 11):\n    if n % 3 == 0 and n % 5 == 0:\n        print('FizzBuzz')\n    elif n % 3 == 0:\n        print('Fizz')\n    elif n % 5 == 0:\n        print('Buzz')\n    else:\n        print(n)",
        "advanced_example": "The loop `else` clause is perfect for search problems — it runs only when no `break` happened:\n\ndef find_prime(n):\n    for d in range(2, int(n ** 0.5) + 1):\n        if n % d == 0:\n            return f'{n} = {d} x {n // d}'\n    else:\n        return f'{n} is prime'\n\nNested comprehensions can flatten matrices:\n    flat = [cell for row in matrix for cell in row]",
        "key_points": "• Indentation, not braces, defines a block\n• `range(start, stop, step)` excludes `stop`\n• Never mutate a list while iterating over it — iterate a copy\n• `break` exits, `continue` skips, loop `else` runs if no break\n• Prefer comprehensions over manual accumulate-and-append loops",
        "resources": "• Python Tutorial — More Control Flow Tools\n• 'Python Crash Course' chapters 5-7\n• Real Python — Comprehensions guide\n• LeetCode easy problems for loop practice",
        "study_time_minutes": 35,
    },
    {
        "name": "Functions and Modules",
        "difficulty": "medium",
        "order_number": 3,
        "description": "Defining functions, arguments, scope, return values, and importing modules.",
        "introduction": "Functions package reusable logic behind a name. Modules group functions into files you can import. Together they are how Python programs stay organised as they grow.",
        "explanation": "**Defining a function**\n    def greet(name, greeting='Hello'):\n        return f'{greeting}, {name}!'\n\n**Argument kinds**\n- positional: `greet('Ada')`\n- keyword: `greet('Ada', greeting='Hi')`\n- `*args`: collects extra positional args into a tuple\n- `**kwargs`: collects extra keyword args into a dict\n\n**Scope**\nNames created inside a function are local. Reading a global works, but assigning one needs `global` (usually a design smell — prefer returning a value).\n\n**Return values**\nA function with no `return` returns `None`. Multiple values come back as a tuple:\n    return total, count\n\n**Modules**\nAny `.py` file is a module. `import math`, `from math import sqrt`, or `import numpy as np`. `if __name__ == '__main__':` guards code that should only run when the file is executed directly, not imported.",
        "basic_example": "def area(length, width):\n    return length * width\n\nprint(area(5, 3))            # 15\nprint(area(width=3, length=5))  # 15 — keyword order does not matter",
        "advanced_example": "Default arguments are evaluated ONCE at definition time, so a mutable default is shared across calls — a classic bug:\n\ndef add_item(item, bucket=[]):   # WRONG\n    bucket.append(item)\n    return bucket\n\nadd_item(1)  # [1]\nadd_item(2)  # [1, 2]  <- surprise!\n\nThe fix is a None sentinel:\n\ndef add_item(item, bucket=None):\n    if bucket is None:\n        bucket = []\n    bucket.append(item)\n    return bucket",
        "key_points": "• `def` creates a function object; `return` exits with a value\n• Default parameters must be immutable (use `None` for lists/dicts)\n• `*args` and `**kwargs` accept variable-length arguments\n• Local scope is per-call; avoid `global` where a return value works\n• `from module import name` vs `import module` — keep imports explicit",
        "resources": "• Python Tutorial — Defining Functions\n• Real Python — *args and **kwargs\n• 'Fluent Python' chapter on functions\n• Python Module of the Week (pymotw.com)",
        "study_time_minutes": 40,
    },
    {
        "name": "Data Structures",
        "difficulty": "medium",
        "order_number": 4,
        "description": "Lists, tuples, dictionaries, and sets — when to use each and their costs.",
        "introduction": "Python's four built-in collections — list, tuple, dict, and set — cover the vast majority of programming needs. Choosing the right one is mostly a question of mutability, ordering, and lookup cost.",
        "explanation": "**list** — ordered, mutable, allows duplicates\n    xs = [1, 2, 3]\n    xs.append(4); xs[0] = 9\nIndexing is O(1); searching with `in` is O(n).\n\n**tuple** — ordered, immutable\n    point = (3, 4)\nHashable if its contents are, so it can be a dict key.\n\n**dict** — key to value, insertion-ordered since 3.7\n    ages = {'ada': 36}\n    ages['grace'] = 45\n    ages.get('alan', 0)      # no KeyError\nAverage lookup is O(1).\n\n**set** — unordered, unique, hashable members\n    s = {1, 2, 3}\nSupports union `|`, intersection `&`, difference `-`. Membership is O(1).\n\n**Slicing** works on all sequences: `xs[1:4]`, `xs[::-1]` (reverse).\n\n**Mutability matters**: lists and dicts are passed by reference, so a function can modify the caller's object. Use `copy()` or a tuple when that is not intended.",
        "basic_example": "scores = {'math': 90, 'science': 85}\nfor subject, score in scores.items():\n    print(f'{subject}: {score}')\n\nprint('Average:', sum(scores.values()) / len(scores))",
        "advanced_example": "Using a set instead of a list turns an O(n^2) duplicate check into O(n):\n\nseen = set()\nunique = []\nfor x in data:\n    if x not in seen:      # O(1) instead of O(n)\n        seen.add(x)\n        unique.append(x)\n\ndict.setdefault and collections.defaultdict remove the 'is the key present?' dance:\n\n    groups = {}\n    for name, dept in rows:\n        groups.setdefault(dept, []).append(name)",
        "key_points": "• list = ordered + mutable; tuple = ordered + immutable\n• dict lookup and set membership are O(1); list `in` is O(n)\n• Only hashable (immutable) objects can be dict keys or set members\n• Slicing returns a new object — `xs[:]` is a shallow copy\n• Collections are passed by reference; copy before mutating in a function",
        "resources": "• Python Tutorial — Data Structures\n• Real Python — Dictionaries and Sets in depth\n• 'Python Cookbook' — data structures recipes\n• Big-O cheat sheet for the cost of each operation",
        "study_time_minutes": 40,
    },
    {
        "name": "File Handling and Exceptions",
        "difficulty": "medium",
        "order_number": 5,
        "description": "Reading and writing files, context managers, and robust error handling.",
        "introduction": "Real programs read input and fail. Python's `with` statement guarantees files are closed, and `try/except` lets you handle failures explicitly instead of crashing.",
        "explanation": "**Opening files**\n    with open('data.txt', 'r', encoding='utf-8') as f:\n        for line in f:\n            print(line.strip())\n\nModes: `r` read, `w` write (truncates), `a` append, `x` create-only; add `b` for binary. The `with` block closes the file even if an exception is raised.\n\n**Structured formats**\n    import json, csv\n    data = json.load(open('c.json'))\n    rows = list(csv.DictReader(open('r.csv')))\n\n**Exceptions**\n    try:\n        value = int(user_input)\n    except ValueError:\n        print('Not a number')\n    else:\n        print('Parsed ok')\n    finally:\n        cleanup()\n\nCatch the *specific* exception. A bare `except:` hides bugs including KeyboardInterrupt.\n\n**Raising your own**\n    if not items:\n        raise ValueError('cart is empty')",
        "basic_example": "with open('notes.txt', 'w') as f:\n    f.write('Line one\\n')\n    f.write('Line two\\n')\n\nwith open('notes.txt') as f:\n    print(f.read())",
        "advanced_example": "EAFP — 'Easier to Ask Forgiveness than Permission' — is the idiomatic Python style. Instead of checking first, attempt the operation and handle failure:\n\n    # Not idiomatic\n    if key in d:\n        use(d[key])\n\n    # Idiomatic\n    try:\n        use(d[key])\n    except KeyError:\n        use_default()\n\nFor reusable cleanup logic, `contextlib.contextmanager` turns a generator into a context manager:\n\n    @contextmanager\n    def timer(label):\n        start = time.perf_counter()\n        yield\n        print(f'{label}: {time.perf_counter() - start:.3f}s')",
        "key_points": "• Always use `with open(...)` so files close on error too\n• Specify `encoding='utf-8'` explicitly for text files\n• Catch specific exceptions; never use a bare `except:`\n• `else` runs when no exception occurred; `finally` always runs\n• EAFP (try it) is preferred over LBYL (check first)",
        "resources": "• Python Tutorial — Errors and Exceptions\n• Real Python — Working with Files\n• Python docs — contextlib\n• 'Effective Python' items on exception handling",
        "study_time_minutes": 35,
    },
]

QUESTIONS = {
    "Introduction to Python": [
        {"question": "Python is best described as:", "option_a": "A compiled, statically typed language", "option_b": "An interpreted, dynamically typed language", "option_c": "A markup language", "option_d": "A database query language", "correct_answer": "B", "explanation": "Python is executed by an interpreter and types are checked at runtime, not compile time."},
        {"question": "What is the result of 7 / 2 in Python 3?", "option_a": "3", "option_b": "3.5", "option_c": "4", "option_d": "1", "correct_answer": "B", "explanation": "Single slash is true division and always returns a float; 7 // 2 would be 3."},
        {"question": "Which expression computes 2 to the power of 5?", "option_a": "2 ^ 5", "option_b": "2 ** 5", "option_c": "pow(2, 5) only", "option_d": "2 ^^ 5", "correct_answer": "B", "explanation": "** is the exponent operator (^ is bitwise XOR in Python). pow(2, 5) also works, but B is the operator form."},
        {"question": "What does input() return?", "option_a": "An int", "option_b": "A float", "option_c": "A string", "option_d": "The type the user typed", "correct_answer": "C", "explanation": "input() always returns str; convert with int() or float() if you need a number."},
        {"question": "How are code blocks delimited in Python?", "option_a": "Curly braces { }", "option_b": "begin/end keywords", "option_c": "Consistent indentation", "option_d": "Semicolons", "correct_answer": "C", "explanation": "Indentation is syntactically significant in Python and defines blocks."},
        {"question": "Which is NOT a built-in Python data type?", "option_a": "list", "option_b": "tuple", "option_c": "array", "option_d": "dict", "correct_answer": "C", "explanation": "There is no built-in 'array' type; the list serves that role. The array module exists but is separate."},
        {"question": "What is the output of print(f'{3 + 4}')?", "option_a": "{3 + 4}", "option_b": "f7", "option_c": "7", "option_d": "34", "correct_answer": "C", "explanation": "The expression inside {} is evaluated and interpolated, giving 7."},
        {"question": "Which statement about variables is true?", "option_a": "Types must be declared before use", "option_b": "A name can be rebound to a different type", "option_c": "Variables must be constants", "option_d": "Names cannot be reassigned", "correct_answer": "B", "explanation": "Python is dynamically typed, so a name can point to values of different types over time."},
        {"question": "What is 10 % 3?", "option_a": "3", "option_b": "1", "option_c": "0", "option_d": "3.33", "correct_answer": "B", "explanation": "% is the modulus operator, returning the remainder: 10 = 3*3 + 1."},
        {"question": "Which function reports an object's type?", "option_a": "typeof(x)", "option_b": "type(x)", "option_c": "x.type()", "option_d": "getClass(x)", "correct_answer": "B", "explanation": "type(x) returns the class of x; isinstance(x, T) tests membership."},
    ],
    "Control Flow": [
        {"question": "How many times does `for i in range(3)` iterate?", "option_a": "2", "option_b": "3", "option_c": "4", "option_d": "1", "correct_answer": "B", "explanation": "range(3) yields 0, 1, 2 — the stop value is exclusive."},
        {"question": "What does `break` do inside a loop?", "option_a": "Skips the current iteration", "option_b": "Restarts the loop", "option_c": "Exits the loop immediately", "option_d": "Pauses execution", "correct_answer": "C", "explanation": "break terminates the innermost loop right away; continue skips to the next iteration."},
        {"question": "When does a `for ... else:` block execute?", "option_a": "Always, after the loop", "option_b": "Only if the loop body raised an error", "option_c": "Only if the loop completed without break", "option_d": "Never", "correct_answer": "C", "explanation": "The else clause runs when the loop finishes normally, i.e. no break was hit."},
        {"question": "What does `[x*2 for x in range(3)]` produce?", "option_a": "[0, 2, 4]", "option_b": "[2, 4, 6]", "option_c": "[0, 1, 2]", "option_d": "[1, 2, 3]", "correct_answer": "A", "explanation": "range(3) gives 0,1,2 and each is doubled, giving [0, 2, 4]."},
        {"question": "Which keyword skips the rest of the current iteration?", "option_a": "break", "option_b": "continue", "option_c": "pass", "option_d": "return", "correct_answer": "B", "explanation": "continue jumps to the next iteration of the loop."},
        {"question": "A `while` loop stops when:", "option_a": "It has run 10 times", "option_b": "Its condition becomes False", "option_c": "It reaches the end of the file", "option_d": "A break is required", "correct_answer": "B", "explanation": "while repeats as long as its condition is truthy and stops when it becomes False."},
        {"question": "What is the risk of modifying a list while iterating over it?", "option_a": "It is faster", "option_b": "Elements can be skipped or an error raised", "option_c": "Nothing, it is safe", "option_d": "The list is sorted", "correct_answer": "B", "explanation": "Mutating the iterable shifts indices, so items get skipped; iterate a copy or use a comprehension."},
        {"question": "`elif` is short for:", "option_a": "else if", "option_b": "else final", "option_c": "elif is not valid", "option_d": "end if", "correct_answer": "A", "explanation": "elif means 'else if' and chains additional conditions after if."},
        {"question": "Which produces the numbers 10 down to 1?", "option_a": "range(10, 0, -1)", "option_b": "range(1, 10)", "option_c": "range(10, 1)", "option_d": "range(0, 10, -1)", "correct_answer": "A", "explanation": "range(10, 0, -1) yields 10, 9, ..., 1; the stop value 0 is excluded."},
        {"question": "What does `pass` do?", "option_a": "Exits the function", "option_b": "Nothing — it is a placeholder", "option_c": "Skips the loop", "option_d": "Raises an error", "correct_answer": "B", "explanation": "pass is a no-op used where syntax requires a statement but no action is needed."},
    ],
    "Functions and Modules": [
        {"question": "Which keyword defines a function?", "option_a": "func", "option_b": "def", "option_c": "function", "option_d": "lambda only", "correct_answer": "B", "explanation": "def defines a named function; lambda creates an anonymous one-liner."},
        {"question": "What does a function return if it has no return statement?", "option_a": "0", "option_b": "An empty string", "option_c": "None", "option_d": "It raises an error", "correct_answer": "C", "explanation": "Falling off the end of a function implicitly returns None."},
        {"question": "Why is `def f(x, items=[])` problematic?", "option_a": "It is invalid syntax", "option_b": "The default list is created once and shared across calls", "option_c": "Lists cannot be parameters", "option_d": "It makes the function slower", "correct_answer": "B", "explanation": "Default values are evaluated at definition time, so the same list object is reused; use None instead."},
        {"question": "What does `*args` collect?", "option_a": "Keyword arguments into a dict", "option_b": "Extra positional arguments into a tuple", "option_c": "A single list", "option_d": "Global variables", "correct_answer": "B", "explanation": "*args gathers additional positional arguments into a tuple; **kwargs does the dict equivalent."},
        {"question": "What does `**kwargs` collect?", "option_a": "Positional args into a tuple", "option_b": "Keyword args into a dict", "option_c": "Only two arguments", "option_d": "Nothing", "correct_answer": "B", "explanation": "**kwargs gathers extra keyword arguments into a dictionary."},
        {"question": "What does `if __name__ == '__main__':` guard against?", "option_a": "Syntax errors", "option_b": "Code running when the module is imported", "option_c": "Infinite recursion", "option_d": "Name collisions", "correct_answer": "B", "explanation": "The block runs only when the file is executed directly, not when it is imported elsewhere."},
        {"question": "Assigning to a global variable inside a function requires:", "option_a": "Nothing special", "option_b": "The global keyword", "option_c": "The static keyword", "option_d": "It is impossible", "correct_answer": "B", "explanation": "Without `global`, assignment creates a new local name that shadows the global one."},
        {"question": "`return a, b` actually returns:", "option_a": "Two separate values", "option_b": "A tuple containing a and b", "option_c": "Only a", "option_d": "A list", "correct_answer": "B", "explanation": "Comma-separated values are packed into a tuple, which can then be unpacked by the caller."},
        {"question": "Which import form binds only `sqrt` into the namespace?", "option_a": "import math", "option_b": "from math import sqrt", "option_c": "import sqrt from math", "option_d": "using math.sqrt", "correct_answer": "B", "explanation": "`from math import sqrt` binds the name sqrt directly."},
        {"question": "Are arguments passed by value or reference in Python?", "option_a": "Always by value", "option_b": "Always by reference", "option_c": "Object references are passed, so mutable objects can be modified", "option_d": "Neither", "correct_answer": "C", "explanation": "Python passes references to objects; mutating a mutable argument affects the caller's object, but rebinding the name does not."},
    ],
    "Data Structures": [
        {"question": "Which collection is immutable?", "option_a": "list", "option_b": "dict", "option_c": "tuple", "option_d": "set", "correct_answer": "C", "explanation": "Tuples cannot be modified after creation; lists, dicts, and sets are mutable."},
        {"question": "Average time complexity of a dict key lookup is:", "option_a": "O(n)", "option_b": "O(1)", "option_c": "O(log n)", "option_d": "O(n log n)", "correct_answer": "B", "explanation": "Dicts are hash tables, so lookup, insert, and delete are O(1) on average."},
        {"question": "Membership testing with `in` on a list is:", "option_a": "O(1)", "option_b": "O(n)", "option_c": "O(log n)", "option_d": "O(n^2)", "correct_answer": "B", "explanation": "Lists are scanned linearly; use a set for O(1) membership tests."},
        {"question": "Which can be used as a dictionary key?", "option_a": "A list", "option_b": "A dict", "option_c": "A tuple of strings", "option_d": "A set", "correct_answer": "C", "explanation": "Keys must be hashable; tuples of immutable items are hashable, while lists/dicts/sets are not."},
        {"question": "What does `xs[::-1]` return?", "option_a": "The first element", "option_b": "A reversed copy of xs", "option_c": "The last element", "option_d": "An error", "correct_answer": "B", "explanation": "A slice with step -1 produces a new reversed list."},
        {"question": "Which method adds an item to the end of a list?", "option_a": "list.add()", "option_b": "list.append()", "option_c": "list.push()", "option_d": "list.insert_end()", "correct_answer": "B", "explanation": "append() adds one item at the end; extend() adds all items from another iterable."},
        {"question": "Which set operator gives the intersection?", "option_a": "|", "option_b": "&", "option_c": "-", "option_d": "^", "correct_answer": "B", "explanation": "& is intersection, | is union, - is difference, ^ is symmetric difference."},
        {"question": "What does `d.get('k', 0)` do when 'k' is missing?", "option_a": "Raises KeyError", "option_b": "Returns 0", "option_c": "Inserts 'k'", "option_d": "Returns None always", "correct_answer": "B", "explanation": "get() returns the default instead of raising; note it does not insert the key."},
        {"question": "Since Python 3.7, dict ordering is:", "option_a": "Sorted by key", "option_b": "Insertion ordered", "option_c": "Random", "option_d": "Reverse alphabetical", "correct_answer": "B", "explanation": "Dicts preserve the order in which keys were inserted."},
        {"question": "If a list is passed to a function and mutated inside, the caller's list:", "option_a": "Is unchanged", "option_b": "Is also changed", "option_c": "Becomes a tuple", "option_d": "Raises an error", "correct_answer": "B", "explanation": "The function receives a reference to the same object, so mutations are visible to the caller."},
    ],
    "File Handling and Exceptions": [
        {"question": "Why use `with open(...)` instead of plain open()?", "option_a": "It is faster", "option_b": "The file is closed automatically, even on error", "option_c": "It reads the whole file into memory", "option_d": "It encrypts the file", "correct_answer": "B", "explanation": "The context manager calls close() on exit, including when an exception is raised."},
        {"question": "Which mode truncates an existing file?", "option_a": "'r'", "option_b": "'w'", "option_c": "'a'", "option_d": "'x'", "correct_answer": "B", "explanation": "'w' overwrites from the start; 'a' appends and 'x' fails if the file exists."},
        {"question": "Catching a bare `except:` is discouraged because:", "option_a": "It is a syntax error", "option_b": "It also swallows KeyboardInterrupt and hides bugs", "option_c": "It slows the program", "option_d": "It cannot log anything", "correct_answer": "B", "explanation": "A bare except catches everything including SystemExit and KeyboardInterrupt; catch specific exceptions."},
        {"question": "Which clause always runs, whether or not an exception occurred?", "option_a": "else", "option_b": "except", "option_c": "finally", "option_d": "try", "correct_answer": "C", "explanation": "finally runs on every path, which makes it suitable for cleanup."},
        {"question": "When does the `else` clause of try/except run?", "option_a": "Always", "option_b": "Only when no exception was raised", "option_c": "Only on exception", "option_d": "Before try", "correct_answer": "B", "explanation": "else runs after try completes without raising, keeping the success path separate."},
        {"question": "Which module reads and writes JSON?", "option_a": "csv", "option_b": "json", "option_c": "pickle only", "option_d": "re", "correct_answer": "B", "explanation": "json.load/json.dump read and write JSON; csv handles comma-separated values."},
        {"question": "What does `raise ValueError('bad input')` do?", "option_a": "Logs a warning", "option_b": "Raises an exception with that message", "option_c": "Returns 'bad input'", "option_d": "Nothing", "correct_answer": "B", "explanation": "raise signals an error that callers can catch with except ValueError."},
        {"question": "EAFP stands for:", "option_a": "Easy And Fast Python", "option_b": "Easier to Ask Forgiveness than Permission", "option_c": "Error Avoidance For Python", "option_d": "Exceptions Are For Programmers", "correct_answer": "B", "explanation": "EAFP means attempting the operation and handling failure, rather than checking preconditions first."},
        {"question": "Why specify `encoding='utf-8'` when opening text files?", "option_a": "It is required syntax", "option_b": "The default depends on the platform and can corrupt text", "option_c": "It makes reading faster", "option_d": "It enables compression", "correct_answer": "B", "explanation": "The default encoding varies by OS/locale, so being explicit avoids decode errors and mojibake."},
        {"question": "Which statement about file iteration is true?", "option_a": "for line in f loads the whole file", "option_b": "for line in f streams one line at a time", "option_c": "Files cannot be iterated", "option_d": "It returns a list of characters", "correct_answer": "B", "explanation": "Iterating a file object yields lines lazily, so memory use stays low on large files."},
    ],
}
