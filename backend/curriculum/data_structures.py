"""Data Structures curriculum (B.Tech 2nd Year / BCA)."""

TOPICS = [
    {
        "name": "Arrays and Complexity Analysis",
        "difficulty": "easy",
        "order_number": 1,
        "description": "Array representation, indexing, and Big-O time/space analysis.",
        "introduction": "An array stores elements of the same type in consecutive memory locations. Because the address of element i can be computed directly, arrays give constant-time access — the foundation every other data structure is measured against.",
        "explanation": "**Memory layout**\n    address(i) = base + i * element_size\nThis formula is why `a[i]` is O(1) regardless of array size.\n\n**Operations and cost**\n- Access by index — O(1)\n- Search (unsorted) — O(n)\n- Insert / delete at the end — O(1) amortised\n- Insert / delete in the middle — O(n), because later elements must shift\n\n**Big-O notation**\nDescribes how cost grows with input size n, ignoring constants:\n- O(1) constant, O(log n) logarithmic, O(n) linear, O(n log n) linearithmic, O(n^2) quadratic\n\n**Best / average / worst case**\nAlways state which you mean. Sorting a sorted array with insertion sort is O(n) best case but O(n^2) worst case.\n\n**Space complexity** counts extra memory, not the input.\n\n**Amortised analysis**: a dynamic array doubles capacity when full, so an individual append is sometimes O(n) but the average over n appends is O(1).",
        "basic_example": "a = [10, 20, 30, 40]\nprint(a[2])        # 30  -> O(1)\nprint(len(a))      # 4   -> O(1)\na.append(50)       # O(1) amortised\na.insert(0, 5)     # O(n) - every element shifts right",
        "advanced_example": "A 2-D matrix stored row-major is cache-friendly when traversed row-wise. Column-wise traversal jumps by a full row each step and causes cache misses, so it can be 10x slower on large matrices even though both are O(n^2):\n\n    for i in range(n):          # fast - sequential access\n        for j in range(n):\n            total += m[i][j]\n\n    for j in range(n):          # slow - strided access\n        for i in range(n):\n            total += m[i][j]",
        "key_points": "• Index access is O(1) because addresses are computed, not searched\n• Mid-array insert/delete is O(n) due to element shifting\n• Big-O describes growth, discarding constants and lower-order terms\n• Distinguish best, average, and worst case\n• Dynamic arrays give O(1) amortised append by doubling capacity",
        "resources": "• 'Introduction to Algorithms' (CLRS) chapter 1-3\n• Big-O Cheat Sheet (bigocheatsheet.com)\n• Abdul Bari — Asymptotic Notation videos\n• Visualgo — array visualisations",
        "study_time_minutes": 40,
    },
    {
        "name": "Linked Lists",
        "difficulty": "medium",
        "order_number": 2,
        "description": "Singly, doubly, and circular linked lists; insertion, deletion, and traversal.",
        "introduction": "A linked list stores elements in nodes, each holding a value and a reference to the next node. Nodes need not be contiguous, so insertion and deletion are cheap — but random access requires walking the chain.",
        "explanation": "**Node**\n    class Node:\n        def __init__(self, data):\n            self.data = data\n            self.next = None\n\n**Singly linked list** — each node points to the next. Insertion at the head is O(1); at the tail is O(n) unless you keep a tail pointer.\n\n**Doubly linked list** — adds a `prev` pointer, so deletion of a known node becomes O(1) and you can traverse backwards. Costs one extra pointer per node.\n\n**Circular linked list** — the last node points back to the head; useful for round-robin scheduling.\n\n**Comparison with arrays**\n- Access by index: array O(1), list O(n)\n- Insert at head: array O(n), list O(1)\n- Memory: arrays are contiguous and cache-friendly; lists allocate per node\n\n**Classic pointer technique**: to delete the nth node from the end in one pass, advance a fast pointer n steps, then move both until fast reaches the end — slow is now at the target.",
        "basic_example": "class Node:\n    def __init__(self, data, nxt=None):\n        self.data = data\n        self.next = nxt\n\nhead = Node(1, Node(2, Node(3)))\n\ncur = head\nwhile cur:\n    print(cur.data)\n    cur = cur.next",
        "advanced_example": "Floyd's cycle detection ('tortoise and hare') finds a loop in O(n) time and O(1) space:\n\ndef has_cycle(head):\n    slow = fast = head\n    while fast and fast.next:\n        slow = slow.next          # 1 step\n        fast = fast.next.next     # 2 steps\n        if slow is fast:\n            return True\n    return False\n\nIf a cycle exists the two pointers must meet; if not, fast reaches the end first. Resetting slow to head afterwards and advancing both one step at a time locates the cycle's entry node.",
        "key_points": "• Nodes hold data plus a next (and optionally prev) pointer\n• Head insertion is O(1); index access is O(n)\n• Doubly linked lists allow O(1) deletion and reverse traversal\n• Always handle the empty-list and single-node edge cases\n• Fast/slow pointer technique solves cycle and midpoint problems",
        "resources": "• 'Data Structures and Algorithms in Python' — Goodrich\n• GeeksforGeeks — Linked List section\n• VisuAlgo — Linked List visualisation\n• LeetCode Linked List problem set",
        "study_time_minutes": 45,
    },
    {
        "name": "Stacks and Queues",
        "difficulty": "easy",
        "order_number": 3,
        "description": "LIFO and FIFO abstract data types and their classic applications.",
        "introduction": "A stack is Last-In-First-Out; a queue is First-In-First-Out. Both restrict access to one end (or one end each), which is exactly what makes them the right tool for recursion, undo, scheduling, and breadth-first search.",
        "explanation": "**Stack (LIFO)** — push and pop both O(1)\nApplications: function call stack, expression evaluation, undo/redo, browser back button, balancing parentheses.\n\n**Queue (FIFO)** — enqueue at the rear, dequeue at the front\nApplications: printer spooling, CPU scheduling, BFS traversal, task queues.\n\n**Implementations**\nA stack can be a list (`append` / `pop`). A queue should NOT be a list — `pop(0)` is O(n) because every element shifts. Use `collections.deque`, whose `appendleft` and `popleft` are O(1).\n\n**Variants**\n- Circular queue — fixed-size array with wrap-around indices\n- Priority queue — dequeue returns the highest-priority item, usually a heap\n- Double-ended queue (deque) — insert and remove at both ends\n\n**Two-stack queue**: push into stack A; to dequeue, pop from B, refilling B from A when empty. Each element moves at most twice, so operations are O(1) amortised.",
        "basic_example": "from collections import deque\n\nstack = []\nstack.append('a')      # push\nstack.append('b')\nprint(stack.pop())     # 'b' - LIFO\n\nq = deque()\nq.append('a')          # enqueue\nq.append('b')\nprint(q.popleft())     # 'a' - FIFO",
        "advanced_example": "Balanced-parentheses checking is the canonical stack problem:\n\ndef is_balanced(s):\n    pairs = {')': '(', ']': '[', '}': '{'}\n    stack = []\n    for ch in s:\n        if ch in '([{':\n            stack.append(ch)\n        elif ch in pairs:\n            if not stack or stack.pop() != pairs[ch]:\n                return False\n    return not stack        # True only if nothing is left over\n\nThe final `not stack` matters: '(((' never mismatches but is still unbalanced.",
        "key_points": "• Stack = LIFO, queue = FIFO; both give O(1) core operations\n• Use collections.deque for queues — list.pop(0) is O(n)\n• Stacks model recursion, undo, and expression parsing\n• Queues model scheduling and breadth-first search\n• A priority queue orders by priority, not arrival time",
        "resources": "• Python docs — collections.deque\n• 'Algorithm Design Manual' — Skiena\n• Visualgo — Stack and Queue\n• LeetCode: Valid Parentheses, Implement Queue using Stacks",
        "study_time_minutes": 35,
    },
    {
        "name": "Trees and Binary Search Trees",
        "difficulty": "hard",
        "order_number": 4,
        "description": "Tree terminology, BST ordering property, traversals, and balancing.",
        "introduction": "A tree is a hierarchical structure with one root and no cycles. A Binary Search Tree keeps smaller keys in the left subtree and larger keys in the right, which turns search into a halving process — O(log n) when balanced.",
        "explanation": "**Terminology**\nroot, parent, child, leaf, height (longest path down), depth (path from root), subtree.\n\n**BST property**\nFor every node: all keys in the left subtree < node.key < all keys in the right subtree. An in-order traversal therefore yields sorted output.\n\n**Traversals** — all O(n)\n- In-order (L, node, R) — sorted order for a BST\n- Pre-order (node, L, R) — copying / serialising a tree\n- Post-order (L, R, node) — deleting a tree, expression evaluation\n- Level-order — breadth-first using a queue\n\n**Performance**\nSearch, insert, delete are O(h) where h is the height. Balanced trees keep h = O(log n). Inserting sorted data into a plain BST degenerates it into a linked list with h = O(n).\n\n**Self-balancing trees** — AVL (strict height balance, ±1), Red-Black (looser, fewer rotations) — restore balance with rotations after insert/delete.\n\n**Heaps** are a different tree: a complete binary tree where each parent is <= (min-heap) or >= (max-heap) its children. They give O(1) peek and O(log n) push/pop, which is what a priority queue needs.",
        "basic_example": "class Node:\n    def __init__(self, key):\n        self.key, self.left, self.right = key, None, None\n\ndef insert(root, key):\n    if root is None:\n        return Node(key)\n    if key < root.key:\n        root.left = insert(root.left, key)\n    else:\n        root.right = insert(root.right, key)\n    return root\n\ndef inorder(root):\n    if root:\n        inorder(root.left)\n        print(root.key, end=' ')\n        inorder(root.right)",
        "advanced_example": "Validating a BST needs a range, not just a parent comparison — comparing only with the parent accepts invalid trees:\n\ndef is_bst(node, lo=float('-inf'), hi=float('inf')):\n    if node is None:\n        return True\n    if not (lo < node.key < hi):\n        return False\n    return (is_bst(node.left, lo, node.key) and\n            is_bst(node.right, node.key, hi))\n\nEvery node in the left subtree must be below the *ancestor's* bound, which the narrowing (lo, hi) window enforces as the recursion descends.",
        "key_points": "• BST invariant: left < node < right, recursively\n• In-order traversal of a BST produces sorted keys\n• Cost is O(height); balanced trees keep height at O(log n)\n• Inserting sorted input degenerates a plain BST to O(n)\n• AVL and Red-Black trees rebalance via rotations\n• Heaps support O(log n) priority queue operations",
        "resources": "• CLRS — Red-Black Trees chapter\n• 3Blue1Brown-style BST visualisations\n• VisuAlgo — BST, AVL, Heap\n• LeetCode: Validate BST, Level Order Traversal, Kth Smallest in BST",
        "study_time_minutes": 50,
    },
    {
        "name": "Hashing and Graphs",
        "difficulty": "hard",
        "order_number": 5,
        "description": "Hash tables and collision handling; graph representations and BFS/DFS.",
        "introduction": "Hashing maps keys to array indices so lookup is O(1) on average. Graphs model relationships between entities and are traversed with BFS or DFS — the basis of shortest paths, connectivity, and topological ordering.",
        "explanation": "**Hash table**\nA hash function converts a key into an index. Collisions are unavoidable, so they must be handled:\n- Chaining — each bucket holds a list of entries; simple, degrades to O(n) if many collide\n- Open addressing — probe for the next free slot (linear probing, quadratic probing, double hashing)\n\n**Load factor** = entries / buckets. Keeping it low (rehashing at ~0.75) preserves O(1) average performance.\n\n**Graph representations**\n- Adjacency matrix — O(1) edge lookup, O(V^2) space; good for dense graphs\n- Adjacency list — O(V + E) space; good for sparse graphs\n\n**Traversals**\n- BFS uses a queue, visits in layers, and finds the shortest path in an unweighted graph — O(V + E)\n- DFS uses a stack or recursion, good for cycle detection and topological sort — O(V + E)\n\n**Directed vs undirected**, weighted vs unweighted. A topological sort orders the vertices of a DAG so every edge points forward; Dijkstra's algorithm extends BFS to weighted graphs with non-negative weights.",
        "basic_example": "from collections import defaultdict, deque\n\ngraph = defaultdict(list)\ngraph['A'] += ['B', 'C']\ngraph['B'] += ['D']\n\ndef bfs(start):\n    seen, order, q = {start}, [], deque([start])\n    while q:\n        node = q.popleft()\n        order.append(node)\n        for nxt in graph[node]:\n            if nxt not in seen:\n                seen.add(nxt)\n                q.append(nxt)\n    return order",
        "advanced_example": "Detecting a cycle in a directed graph needs three colours, not two. A plain 'visited' set reports false positives on a diamond (A->B, A->C, B->D, C->D), which is not a cycle:\n\nWHITE, GREY, BLACK = 0, 1, 2\n\ndef has_cycle(graph):\n    colour = defaultdict(int)\n    def visit(u):\n        colour[u] = GREY                 # on the current recursion stack\n        for v in graph[u]:\n            if colour[v] == GREY:        # back edge -> cycle\n                return True\n            if colour[v] == WHITE and visit(v):\n                return True\n        colour[u] = BLACK                # fully explored\n        return False\n    return any(colour[u] == WHITE and visit(u) for u in graph)\n\nGREY means 'currently on the stack', which is exactly what distinguishes a back edge from a cross edge.",
        "key_points": "• A good hash function spreads keys; collisions must be handled\n• Keep the load factor low and rehash to stay O(1) average\n• Adjacency matrix for dense graphs, adjacency list for sparse\n• BFS (queue) gives unweighted shortest paths; DFS (stack) explores deeply\n• BFS/DFS are both O(V + E)\n• Directed cycle detection needs white/grey/black colouring",
        "resources": "• CLRS — Hash Tables and Graph Algorithms\n• 'Grokking Algorithms' — hash tables and graphs\n• VisuAlgo — Hash Table, BFS/DFS\n• LeetCode: Number of Islands, Course Schedule, Clone Graph",
        "study_time_minutes": 55,
    },
]

QUESTIONS = {
    "Arrays and Complexity Analysis": [
        {"question": "Why is array index access O(1)?", "option_a": "Arrays are sorted", "option_b": "The element address is computed from the base address and index", "option_c": "Arrays are small", "option_d": "The compiler caches them", "correct_answer": "B", "explanation": "address(i) = base + i * size, so no search is needed."},
        {"question": "Inserting at the beginning of an array of size n costs:", "option_a": "O(1)", "option_b": "O(log n)", "option_c": "O(n)", "option_d": "O(n log n)", "correct_answer": "C", "explanation": "Every existing element must shift one position right."},
        {"question": "Big-O notation describes:", "option_a": "Exact runtime in seconds", "option_b": "How cost grows with input size, ignoring constants", "option_c": "Memory usage only", "option_d": "The number of lines of code", "correct_answer": "B", "explanation": "Big-O captures growth rate and discards constant factors and lower-order terms."},
        {"question": "Appending to a dynamic array is O(1) because:", "option_a": "It never resizes", "option_b": "Capacity doubles when full, making the cost amortised", "option_c": "It uses a linked list", "option_d": "It is always sorted", "correct_answer": "B", "explanation": "Occasional O(n) resizes are spread over many appends, giving O(1) amortised."},
        {"question": "Which is asymptotically fastest as n grows?", "option_a": "O(n^2)", "option_b": "O(n log n)", "option_c": "O(2^n)", "option_d": "O(n!)", "correct_answer": "B", "explanation": "n log n grows slower than n^2, 2^n, and n!."},
        {"question": "Worst-case time to search an unsorted array of n elements is:", "option_a": "O(1)", "option_b": "O(log n)", "option_c": "O(n)", "option_d": "O(n^2)", "correct_answer": "C", "explanation": "You may have to inspect every element."},
        {"question": "Space complexity measures:", "option_a": "Total file size", "option_b": "Extra memory used beyond the input", "option_c": "Disk space", "option_d": "Number of variables", "correct_answer": "B", "explanation": "It counts auxiliary memory allocated by the algorithm."},
        {"question": "Accessing a[100] vs a[0] in the same array:", "option_a": "a[100] is slower", "option_b": "Both are O(1)", "option_c": "a[0] is slower", "option_d": "Depends on the values", "correct_answer": "B", "explanation": "Direct addressing makes any valid index equally fast."},
        {"question": "Best-case time for insertion sort on already sorted input is:", "option_a": "O(n^2)", "option_b": "O(n)", "option_c": "O(log n)", "option_d": "O(1)", "correct_answer": "B", "explanation": "No swaps are needed; a single linear pass suffices."},
        {"question": "Amortised analysis is useful when:", "option_a": "An operation is occasionally expensive but cheap on average", "option_b": "All operations are equally expensive", "option_c": "The algorithm is recursive", "option_d": "Memory is limited", "correct_answer": "A", "explanation": "It averages a rare costly operation over many cheap ones, as with dynamic array growth."},
    ],
    "Linked Lists": [
        {"question": "What does each node in a singly linked list contain?", "option_a": "Only data", "option_b": "Data and a pointer to the next node", "option_c": "Data and an index", "option_d": "Only a pointer", "correct_answer": "B", "explanation": "A node holds its value plus a reference to the successor node."},
        {"question": "Accessing the k-th element of a linked list is:", "option_a": "O(1)", "option_b": "O(k)", "option_c": "O(log n)", "option_d": "O(n^2)", "correct_answer": "B", "explanation": "You must walk from the head through k nodes; in the worst case this is O(n)."},
        {"question": "Insertion at the head of a linked list (pointer already known) is:", "option_a": "O(n)", "option_b": "O(1)", "option_c": "O(log n)", "option_d": "O(n log n)", "correct_answer": "B", "explanation": "Only the new node's next pointer and the head reference change."},
        {"question": "What advantage does a doubly linked list have?", "option_a": "Less memory", "option_b": "Backward traversal and O(1) deletion of a known node", "option_c": "Faster index access", "option_d": "No pointers needed", "correct_answer": "B", "explanation": "The prev pointer allows moving backwards and unlinking a node without finding its predecessor."},
        {"question": "In a circular linked list, the last node points to:", "option_a": "NULL", "option_b": "The head node", "option_c": "Itself only", "option_d": "The previous node", "correct_answer": "B", "explanation": "The tail's next points back to the head, forming a ring."},
        {"question": "Compared with arrays, linked lists are worse at:", "option_a": "Insertion at the front", "option_b": "Random access by index", "option_c": "Dynamic size", "option_d": "Deletion at the front", "correct_answer": "B", "explanation": "Arrays give O(1) index access; linked lists need O(n) traversal."},
        {"question": "Floyd's cycle detection uses:", "option_a": "Two pointers moving at different speeds", "option_b": "A hash table of visited nodes", "option_c": "Recursion only", "option_d": "Binary search", "correct_answer": "A", "explanation": "The slow and fast pointers meet inside a cycle, using O(1) extra space."},
        {"question": "Deleting the last node of a singly linked list of size n is:", "option_a": "O(1)", "option_b": "O(n)", "option_c": "O(log n)", "option_d": "Impossible", "correct_answer": "B", "explanation": "You must traverse to the second-to-last node to update its next pointer."},
        {"question": "Which edge case must every linked list routine handle?", "option_a": "Negative indices", "option_b": "The empty list (head is None)", "option_c": "Duplicate values", "option_d": "Sorted input", "correct_answer": "B", "explanation": "Dereferencing None on an empty list is the most common linked list bug."},
        {"question": "Keeping a tail pointer makes which operation O(1)?", "option_a": "Index access", "option_b": "Insertion at the end", "option_c": "Searching", "option_d": "Sorting", "correct_answer": "B", "explanation": "Without a tail pointer you must traverse the whole list to append."},
    ],
    "Stacks and Queues": [
        {"question": "A stack follows which order?", "option_a": "FIFO", "option_b": "LIFO", "option_c": "LILO", "option_d": "Random", "correct_answer": "B", "explanation": "Last In, First Out — the most recently pushed item is popped first."},
        {"question": "A queue follows which order?", "option_a": "LIFO", "option_b": "FIFO", "option_c": "Priority only", "option_d": "Sorted order", "correct_answer": "B", "explanation": "First In, First Out — elements leave in the order they arrived."},
        {"question": "Why avoid using a Python list as a queue?", "option_a": "Lists cannot store mixed types", "option_b": "pop(0) is O(n) because elements shift", "option_c": "Lists are immutable", "option_d": "Lists cannot grow", "correct_answer": "B", "explanation": "Removing from the front shifts every remaining element; collections.deque does it in O(1)."},
        {"question": "Which application uses a stack?", "option_a": "Printer job spooling", "option_b": "Function call management and undo", "option_c": "CPU round-robin scheduling", "option_d": "Breadth-first search", "correct_answer": "B", "explanation": "Call frames and undo histories are naturally LIFO; the others are FIFO."},
        {"question": "Which traversal uses a queue?", "option_a": "DFS", "option_b": "BFS", "option_c": "In-order", "option_d": "Post-order", "correct_answer": "B", "explanation": "BFS explores level by level using a FIFO queue."},
        {"question": "Push and pop on a stack are:", "option_a": "O(n)", "option_b": "O(1)", "option_c": "O(log n)", "option_d": "O(n log n)", "correct_answer": "B", "explanation": "Both operate on a single end, so cost does not depend on size."},
        {"question": "A priority queue removes:", "option_a": "The oldest element", "option_b": "The highest-priority element", "option_c": "The largest index", "option_d": "A random element", "correct_answer": "B", "explanation": "Order is determined by priority, not insertion time; usually implemented with a heap."},
        {"question": "Checking balanced parentheses naturally uses:", "option_a": "A queue", "option_b": "A stack", "option_c": "A hash table", "option_d": "A binary tree", "correct_answer": "B", "explanation": "Each closing bracket must match the most recent unmatched opener — LIFO."},
        {"question": "In a circular queue, the rear index:", "option_a": "Grows without bound", "option_b": "Wraps around using modulo", "option_c": "Stays at zero", "option_d": "Is always the front", "correct_answer": "B", "explanation": "Indices wrap with (i + 1) % capacity, reusing freed slots."},
        {"question": "A deque supports:", "option_a": "Insertion at one end only", "option_b": "Insertion and removal at both ends", "option_c": "Sorted insertion only", "option_d": "Random access by index only", "correct_answer": "B", "explanation": "A double-ended queue allows O(1) operations at both the front and the back."},
    ],
    "Trees and Binary Search Trees": [
        {"question": "In a BST, for any node, all keys in its left subtree are:", "option_a": "Greater", "option_b": "Smaller", "option_c": "Equal", "option_d": "Unordered", "correct_answer": "B", "explanation": "The BST invariant is left < node < right, applied recursively."},
        {"question": "Which traversal of a BST yields sorted keys?", "option_a": "Pre-order", "option_b": "In-order", "option_c": "Post-order", "option_d": "Level-order", "correct_answer": "B", "explanation": "In-order visits left subtree, node, right subtree, which follows the ordering invariant."},
        {"question": "Search cost in a BST depends on:", "option_a": "The number of leaves", "option_b": "The height of the tree", "option_c": "The key values", "option_d": "The insertion order only", "correct_answer": "B", "explanation": "Each comparison descends one level, so cost is O(height)."},
        {"question": "Inserting already sorted data into a plain BST results in:", "option_a": "A perfectly balanced tree", "option_b": "A degenerate tree shaped like a linked list", "option_c": "An error", "option_d": "A heap", "correct_answer": "B", "explanation": "Every new key goes right, so height becomes n and operations degrade to O(n)."},
        {"question": "Which tree keeps height at O(log n) automatically?", "option_a": "Plain BST", "option_b": "AVL tree", "option_c": "Linked list", "option_d": "Binary heap", "correct_answer": "B", "explanation": "AVL trees rebalance with rotations to keep the height logarithmic; Red-Black trees do too."},
        {"question": "Pre-order traversal visits nodes in the order:", "option_a": "Left, node, right", "option_b": "Node, left, right", "option_c": "Left, right, node", "option_d": "Level by level", "correct_answer": "B", "explanation": "Pre-order processes the node before its subtrees — useful for copying or serialising."},
        {"question": "In a min-heap, the root holds:", "option_a": "The largest element", "option_b": "The smallest element", "option_c": "The median", "option_d": "The most recent element", "correct_answer": "B", "explanation": "Every parent is less than or equal to its children, so the minimum is at the root."},
        {"question": "Deleting a tree is most naturally done with which traversal?", "option_a": "Pre-order", "option_b": "In-order", "option_c": "Post-order", "option_d": "Level-order", "correct_answer": "C", "explanation": "Post-order deletes children before their parent, so no dangling references remain."},
        {"question": "Validating a BST requires:", "option_a": "Comparing each node with its parent only", "option_b": "Checking each node against a narrowing allowed range", "option_c": "Sorting the nodes", "option_d": "Counting leaves", "correct_answer": "B", "explanation": "A node must respect every ancestor's bound, not just its immediate parent."},
        {"question": "Heap push and pop are:", "option_a": "O(1)", "option_b": "O(log n)", "option_c": "O(n)", "option_d": "O(n log n)", "correct_answer": "B", "explanation": "Each operation may travel from the root to a leaf (or back), which is O(log n) in a complete binary tree."},
    ],
    "Hashing and Graphs": [
        {"question": "Average-case lookup in a hash table is:", "option_a": "O(n)", "option_b": "O(1)", "option_c": "O(log n)", "option_d": "O(n log n)", "correct_answer": "B", "explanation": "The hash function computes the bucket directly, so lookup is constant on average."},
        {"question": "Separate chaining handles collisions by:", "option_a": "Deleting the old entry", "option_b": "Storing a list of entries in each bucket", "option_c": "Resizing immediately", "option_d": "Sorting the table", "correct_answer": "B", "explanation": "Each bucket holds a chain of colliding entries; open addressing probes for another slot instead."},
        {"question": "The load factor of a hash table is:", "option_a": "Buckets / entries", "option_b": "Entries / buckets", "option_c": "Total memory used", "option_d": "Number of collisions", "correct_answer": "B", "explanation": "A high load factor increases collisions, so tables rehash around 0.75."},
        {"question": "Which graph representation uses O(V + E) space?", "option_a": "Adjacency matrix", "option_b": "Adjacency list", "option_c": "Edge list only", "option_d": "Incidence matrix", "correct_answer": "B", "explanation": "Adjacency lists store only existing edges, so sparse graphs are far cheaper than an O(V^2) matrix."},
        {"question": "BFS on an unweighted graph finds:", "option_a": "The longest path", "option_b": "The shortest path in edges", "option_c": "A spanning tree only", "option_d": "The minimum spanning tree", "correct_answer": "B", "explanation": "Because BFS expands in layers, the first time a node is reached is via a minimum-edge path."},
        {"question": "Time complexity of BFS or DFS is:", "option_a": "O(V)", "option_b": "O(E)", "option_c": "O(V + E)", "option_d": "O(V * E)", "correct_answer": "C", "explanation": "Each vertex is visited once and each edge is examined once."},
        {"question": "A topological sort is possible only on:", "option_a": "Any graph", "option_b": "A directed acyclic graph (DAG)", "option_c": "Complete graphs", "option_d": "Undirected graphs", "correct_answer": "B", "explanation": "A cycle makes the ordering contradictory, so the graph must be acyclic."},
        {"question": "Detecting a cycle in a directed graph requires:", "option_a": "A visited set only", "option_b": "Tracking nodes on the current recursion stack (white/grey/black)", "option_c": "Sorting the vertices", "option_d": "Counting edges", "correct_answer": "B", "explanation": "A plain visited set flags diamonds as cycles; a GREY state identifies true back edges."},
        {"question": "Dijkstra's algorithm requires:", "option_a": "Non-negative edge weights", "option_b": "Negative weights only", "option_c": "An unweighted graph", "option_d": "A bipartite graph", "correct_answer": "A", "explanation": "Negative weights can invalidate already-finalised distances; Bellman-Ford handles those."},
        {"question": "Which is a typical hash table application?", "option_a": "Finding shortest paths", "option_b": "Counting word frequencies", "option_c": "Balancing parentheses", "option_d": "Sorting a linked list", "correct_answer": "B", "explanation": "Keyed counting and deduplication are exactly what O(1) lookup is for."},
    ],
}
